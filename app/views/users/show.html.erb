<div class="main user-show">
  <div class="container">
    <div class="user">
      <h2><%= @user.name %></h2>
      <p><%= @user.email %></p>
      <!-- ユーザー編集ページへのリンクを作成してください -->
      <%= link_to("編集", "/users/#{@user.id}/edit") %>
    </div>
  </div>
</div>
